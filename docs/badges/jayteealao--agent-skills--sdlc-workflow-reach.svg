<svg xmlns="http://www.w3.org/2000/svg" width="600" height="20" role="img" aria-label="reach: L3 network, writes the prompt box, reads files, reads env vars, persists state, draws">
<title>reach: L3 network, writes the prompt box, reads files, reads env vars, persists state, draws</title>
<rect width="44" height="20" fill="#555"/><rect x="44" width="556" height="20" fill="#8250df"/>
<g fill="#fff" font-family="Verdana,DejaVu Sans,sans-serif" font-size="11" text-anchor="middle">
<text x="22" y="14">reach</text><text x="322" y="14">L3 network, writes the prompt box, reads files, reads env vars, persists state, draws</text></g></svg>
